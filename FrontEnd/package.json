{
  "name": "spring-msa-frontend",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "24.19.0",
    "pnpm": "10.0.0"
  },
  "scripts": {
    "contracts:generate": "pnpm --filter @springmsa/api-contract generate",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
