apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-member-community-service
  namespace: spring-msa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-member-community-service
  template:
    metadata:
      labels:
        app: spring-member-community-service
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: spring-member-community-service
          image: ghcr.io/hyunmyungchoi/spring-member-community-service@sha256:e38891cb8561a4825da9441ecd3b08ecac8f98ce111719b85c8cc134f7b38226
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=70 -XX:InitialRAMPercentage=30"
            - name: SERVER_TOMCAT_ACCESSLOG_ENABLED
              value: "true"
            - name: SERVER_TOMCAT_ACCESSLOG_DIRECTORY
              value: /dev
            - name: SERVER_TOMCAT_ACCESSLOG_PREFIX
              value: stdout
            - name: SERVER_TOMCAT_ACCESSLOG_SUFFIX
              value: ""
            - name: SERVER_TOMCAT_ACCESSLOG_FILE_DATE_FORMAT
              value: ""
            - name: SERVER_TOMCAT_ACCESSLOG_BUFFERED
              value: "false"
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI
              value: http://user.localtest.me
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI
              value: http://spring-security-authorization-server:9000/oauth2/jwks
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 512m
              memory: 768Mi
            limits:
              cpu: 512m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: spring-member-community-service
  namespace: spring-msa
spec:
  type: ClusterIP
  selector:
    app: spring-member-community-service
  ports:
    - port: 8083
      targetPort: 8083

