server:
  port: 8083

spring:
  application:
    name: spring-community-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  charset:
    console: UTF-8
  level:
    com.springmsa.communityservice: debug
    org.springframework.security: debug
