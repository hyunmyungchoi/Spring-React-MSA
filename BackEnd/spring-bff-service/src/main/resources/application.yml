spring:
  session:
    store-type: redis
    data:
      redis:
        namespace: spring:session:bff

  data:
    redis:
      host: localhost
      port: 16379

  application:
    name: spring-bff-service

bff:
  frontend:
    redirect-uri: http://localhost:5173

  oauth2:
    authorization-uri: http://localhost:8080/oauth2/authorize
    token-uri: http://localhost:8080/oauth2/token
    userinfo-uri: http://localhost:8080/userinfo
    client-id: bff-client
    client-secret: ${BFF_CLIENT_SECRET:bff-secret}
    redirect-uri: http://localhost:8080/bff/auth/callback
    end-session-uri: http://localhost:8080/connect/logout
    scope: openid profile

  api:
    community-me-uri: http://localhost:8080/api/community/me
    user-me-uri: http://localhost:8080/api/user/me
    stock-me-uri: http://localhost:8080/api/stock/me

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8079
  servlet:
    session:
      cookie:
        name: BFFSESSIONID
        http-only: true
        same-site: lax
